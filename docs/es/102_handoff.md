# 第 102 轮交接 — 沟通重建: 感知通道打通, 表达通道根因全定位

## 一句话结论

从零重建的字节域模型**感知通道已打通** (中文 hit1 0.47 / hit3 0.66, UTF-8 结构完整),
**表达通道 (自回归生成) 仍未可解码** — 但 6 个根因已全部定位并修复 5 个,
剩余 1 个 (自回归漂移) 需要架构级方案。

## 最终状态 (2026-08-20)

- 感知: 中文 hit1 0.234-0.47 / hit3 0.34-0.66 (依赖检查点)
- 表达: 生成流出现重复模式 (复读), 不可解码 (dec 0.00-0.19)
- z_bind: top-k 稀疏 (4/32 槽), 区分度 0.75→0.48
- 零 NaN, 全 fp16

## 已修复的 6 个根因 (按发现顺序)

| # | 根因 | 修复 | 实证 |
|---|------|------|------|
| 1 | 回声相位污染世界模型 (乱码流进 W_lm) | 回声冻结世界模型 (只学 W_act) | chat101c top-5 位置无关 |
| 2 | 频率去偏压平 logits (训练+生成) | 移除 -6·log(freq) | 任何去偏系数 hit 归零 |
| 3 | bias_lm 范数锁 100 主导读出 | 降到 10 + 更新降 20× | bias[0]=2.3 实证 |
| 4 | W_lm/W1 更新尺度 = 单位向量全量 | 1% 信任域 | 从零初始化噪声淹没 |
| 5 | **W_act 被冻结块误锁 (变化范数 0)** | 提出冻结块 | 2000 步零更新实证 |
| 6 | W_act 信任域用矩阵范数 (√256 缩放) | 按列范数 | 实际步幅 0.3% vs 意图 5% |

## 剩余障碍 (第 7 个根因): 自回归漂移

- 前馈预测 hit1 0.47 (上下文完整), 但自回归生成第一步就偏 (上下文不足/漂移)
- 生成流复读自己的模式 (`\xbd\xe7\x84\xe9\xe7\xbd...` 循环)
- closed_loop 暴露偏差方案已验证: 冻结世界模型后 fe 0.32 不稳定, 回退
- 候选方案: (a) closed_loop 锚定段也参与误差 (目前只看不学); (b) 更长训练
  (当前 8000+ 步, 第 87 轮成功是 85 轮预训练 + 交互); (c) 生成用温度采样 + 复读阻断

## 架构变更 (用户已批准)

- z_bind 竞争: relu(raw-θ) 全激活 → **top-k 稀疏** (k=4, 每位置只留强槽),
  区分度 0.75→0.48, 但 fe 波动略升 (0.04→0.21, 需更长训练稳定)

## 文件

- `model/dense/learning.py` — 第 102 轮全部学习端修复 (冻结/信任域/软目标/对齐)
- `model/dense/forward.py` — top-k 稀疏绑定 + 去偏移除
- `model/dense/network.py` — bias_lm 锁 10 + _freq_act 均匀初始化
- `scripts/chat102.py` — 双相位训练 (感知+回声, 种子锚定, 观测器)
- `scripts/diag_final.py` — 终态双通道诊断
- 检查点: `out/chat102q_step1700.pt` (top-k 稀疏后最健康)

## 纪律

- 全 fp16, 零 .float(), 零 clamp (结构化 pre-norm)
- 观测器/训练脚本同步无去偏口径 (去偏已移除)
- 探针开关验证后应固化默认 (第 101 轮教训)
